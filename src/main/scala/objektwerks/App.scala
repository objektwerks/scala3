package objektwerks

/**
 * No object required for Scala 3 app!
 *
 * To run via terminal: sbt "run Fred Flintstone"
 */
@main def greeting(first: String, last: String): Unit =
  println(s"@main greetings(first: String, last: String) => Greetings, $first $last!")